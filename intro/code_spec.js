// import {vars} from './code';
const expect = require('chai').expect;

describe('Javascript principles', function() {
  it('can define an integer variable', function() {
    expect(true).should.equal(true);
  });
});
